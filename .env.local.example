# Copy this file to .env.local and fill in real values. Never commit .env.local.

# Google Maps JavaScript API key, restricted to your site's HTTP referrers
# in Google Cloud Console (see AGENTS.md for setup steps).
GOOGLE_MAPS_API_KEY=
